pipeline {
    agent any
    stages {
        stage('fetch code') {
            steps {
                sh 'mvn --version'
            }
        }
        stage('build code'){
            steps{
                sh'ls'
            }
        }
          stage('run tests code'){
            steps{
                sh'ls'
            }
        }
    }
}
